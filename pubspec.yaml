name: crossfit_wod
description: CrossFit WOD Generator - 크로스핏 오늘의 운동 생성기
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  uuid: ^4.2.1
  # audioplayers: ^5.2.1  # GStreamer 필요 - 나중에 활성화
  flutter_svg: ^2.0.9
  cupertino_icons: ^1.0.6
  cached_network_image: ^3.3.1
  lottie: ^3.1.0
  http: ^1.1.0
  web: ^0.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.7

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/sounds/
